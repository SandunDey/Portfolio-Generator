{
  "name": "portfolio-generator-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix Backend run dev",
    "start": "npm --prefix Backend start",
    "install:backend": "npm --prefix Backend install"
  }
}
